import { useEffect, useState } from "react"

const useCurrenciesConversions = (currencies: string[]) => {
  const [conversions, setConversions] = useState([])
  const getConversions = async (args: string[]) => {}

  useEffect(() => {}, [])

  return []
}

export default useCurrenciesConversions
